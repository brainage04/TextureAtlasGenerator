name: release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read

jobs:
  distribution:
    permissions:
      contents: write
    uses: brainage04/FabricModdingConventions/.github/workflows/reusable-multiloader-release.yml@v2.4.1
    with:
      additional_artifact_pattern: textureatlasgenerator-neoforge-*.jar
      release_name: ${{ format('TextureAtlasGenerator {0}', github.ref_name) }}
      modrinth_project_id: ${{ vars.MODRINTH_PROJECT_ID }}
      publish_curseforge: false
    secrets:
      release_token: ${{ secrets.GITHUB_TOKEN }}
      modrinth_token: ${{ secrets.MODRINTH_TOKEN }}
